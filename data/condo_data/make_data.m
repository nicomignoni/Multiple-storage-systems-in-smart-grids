% Generate .mat files from the 'energy_data.csv' file.% Only generation and demand data are extracted, considering the % time step equal to 1 hour. T = 24;data = readtable('energy_data.csv');PV   = table2array(data(1:4:end, 6)); % 'Produzione (W)'DP   = table2array(data(1:4:end, 4)); % 'Consumo (W)'% Divide PV and DP into 24-hours blocks% Transform the unit measure from W to kWPV = reshape(PV, T, [])' / 1e3;DP = reshape(DP, T, [])' / 1e3;% ave('energy_data.mat', 'PV', 'DP');